{
  "name": "friend-inviter",
  "version": "0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "web-ext build -s src -a dist",
    "check": "tsc && eslint . && web-ext lint -s src -a dist",
    "dev": "web-ext run -s src -a dist"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/firefox-webext-browser": "^120.0.4",
    "eslint": "^9.35.0",
    "globals": "^16.4.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.43.0",
    "web-ext": "^8.9.0"
  }
}
